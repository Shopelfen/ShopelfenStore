<div style="display: flex; justify-content: end; margin-bottom: 10px; margin-left: 10px; margin-right: 10px">

    {#
    <mt-switch
            :value="filterByActive"
            @input="onToggleFilter"
            label="nicht Installierte Lizenzen ausblenden"
    />
    #}

    <div style="display: flex; gap: 5px; align-items: center">
        <!-- Placeholder für zukünftige Sortieroptionen
        <sw-select-field :value="selectedSortingOption"
                         @update="onChangeSortingOption"
                         size="small">
            <option v-for="(sortingOption, index) in sortingOptions"
                    :key="`sorting-option-${index}`"
                    :value="sortingOption.value">
                {{ sortingOption.name }}
            </option>
        </sw-select-field>
        -->
        <slot name="refresh"></slot>
    </div>


</div>